# Gmail API Credentials
# Get these from Google Cloud Console: https://console.cloud.google.com/

# Path to your OAuth 2.0 credentials file (download from Google Cloud Console)
GOOGLE_CREDENTIALS_PATH=credentials.json

# Path where the OAuth token will be stored after first authentication
GOOGLE_TOKEN_PATH=token.json

# Optional: Your Gmail address (for reference, not required by the API)
# GMAIL_ADDRESS=your-email@gmail.com
